spring:
  application:
    name: edu-cloud
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-configs:
          - data-id: redis.yml
            refresh: true
          - data-id: health-component-common.yml
            refresh: true

server:
  port: 28000
